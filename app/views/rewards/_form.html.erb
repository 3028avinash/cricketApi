<%= form_with(model: reward) do |form| %>
  <% if reward.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(reward.errors.count, "error") %> prohibited this reward from being saved:</h2>

      <ul>
        <% reward.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :logo, style: "display: block" %>
    <%= form.text_field :logo %>
  </div>

  <%
=begin%>
 <div>
    <%= form.label :amount, style: "display: block" %>
    <%= form.text_field :amount %>
  </div> 
<%
=end%>

  <% Reward::TAGS.each do |role|%>
  <%= form.label role %>
  <%= form.check_box :amount, {multiple: true, checked: form.object.amount&.include?(role.to_s)}, role, nil  %>
  <%end%>

  <div>
    <%= form.submit %>
  </div>
<% end %>

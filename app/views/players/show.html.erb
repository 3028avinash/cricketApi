<p style="color: green"><%= notice %></p>

<%= render @player %>

<div>
  <%= link_to "Edit this player", edit_player_path(@player) %> |
  <%= link_to "Back to players", players_path %>

  <%#= button_to "Destroy this player", @player, method: :delete, data:{turbo_method: :delete, turbo_confirm: 'Are you sure?'} %>
    <%#= button_to "Destroy this match", @match, method: :delete, data:{turbo_method: :delete, turbo_confirm: 'Are you sure?'} %>

</div>

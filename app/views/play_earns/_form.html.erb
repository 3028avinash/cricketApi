<%= form_with(model: play_earn) do |form| %>
  <% if play_earn.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(play_earn.errors.count, "error") %> prohibited this play_earn from being saved:</h2>

      <ul>
        <% play_earn.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :blogImage, style: "display: block" %>
    <%= form.text_field :blogImage %>
  </div>

  <div>
    <%= form.label :blogUrl, style: "display: block" %>
    <%= form.text_field :blogUrl %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
